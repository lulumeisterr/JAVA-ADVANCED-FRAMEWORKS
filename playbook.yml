---
- hosts: fiapServerAws
  remote_user: ubuntu
  become: false
  tasks:
    - name: Transfer file from ServerA to ServerB
      synchronize:
        src: /var/lib/jenkins/workspace/Demo/StrategyPatterns
        dest: /ubuntu/StrategyPatterns
        mode: pull
    - name: 'nginx'
      apt: name ={{ item }} state=present
      with_items:
            - nginx state=latest
            - clean all
            
    - name: 'Start nginx server'
      service:
        name: nginx
        state: started
...
